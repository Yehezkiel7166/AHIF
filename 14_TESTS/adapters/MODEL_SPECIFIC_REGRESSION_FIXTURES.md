# Model-Specific Adapter Regression Fixtures

## Fixture A — Kyoto Cold Morning

Expected: identity reference retained; long coat and cold response preserved; quiet Gion context retained; aspect ratio mapped; no unsupported parameter forwarded.

## Fixture B — Tokyo Rain

Expected: rain interaction, wet-surface lighting, practical wardrobe, and identity lock remain present. Negative constraints prevent dry-weather contradiction and generic face replacement.

## Fixture C — Missing Master Photo

Expected: every adapter blocks production-equivalent release. Text-only output may exist only as explicitly degraded experimental diagnostic output.

## Fixture D — Unknown Parameter

Expected: serializer rejects the request and records `ADAPTER_PARAMETER_UNKNOWN`; it does not pass the field through.

## Fixture E — Unsupported Identity Mechanism

Expected: capability resolution returns `blocked`, no executable target request is emitted, and the loss report identifies identity-critical incompatibility.
