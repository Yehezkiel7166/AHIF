# Sprint 027 Repository Health

The executable source is `scripts/repository_health.py`, which generates `reports/repository-health.json` and `reports/repository-health.md` with version, sprint, manifest, workflow, script, registry, LTS, warning/failure, and eligibility fields.

The checked-in baseline is repository-conformant with release eligibility **HOLD** and LTS designation **HOLD**. This is not production or operational evidence.
