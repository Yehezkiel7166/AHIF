# Upload Update — AHIF 1.8.0

## Release

Sprint 008 — Model-Specific Adapter Layer.

## Upload Options

Use the full repository archive to replace the complete repository content, or apply the Sprint 008 patch only when the repository is exactly at version 1.7.0. Do not apply both.

## Verification

After upload, confirm:

- `VERSION.md` reports 1.8.0;
- `manifest.json` reports Sprint 008;
- `16_MODEL_ADAPTERS/REGISTRY/ADAPTER_REGISTRY.json` exists;
- three experimental adapter implementations and profiles exist;
- no adapter is labeled production supported;
- repository paths from earlier versions remain present.
