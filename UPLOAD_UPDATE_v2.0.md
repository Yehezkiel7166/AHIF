# Upload Update — AHIF 2.0.0

## Base

Apply this patch only to an exact `2.0.0-rc2` repository.

## Option A — Full repository

Replace the repository content with the full `2.0.0` archive.

## Option B — Patch

Extract the Sprint 012 patch into the repository root and overwrite matching files.

Do not apply both options.

## Verification

Confirm:

- `VERSION.md` shows `2.0.0`;
- `manifest.json` shows `2.0.0`;
- latest sprint is `SPRINT-012-STABLE-RELEASE`;
- stable release governance and migration documents are present;
- empirical image evidence remains explicitly pending unless independently supplied.
